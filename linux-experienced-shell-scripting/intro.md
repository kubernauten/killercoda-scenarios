
You will write portable **Bash** using `set -euo pipefail`, define a **function**, redirect output to files, and add minimal **argument checks**—patterns you see in real automation and CI scripts.

```text
+-----------------------------+
| Bash script                 |
| set -euo pipefail           |
| functions + argv validation |
| redirects to log files      |
+-----------------------------+
```
