
You can now list pods across the whole cluster with `kubectl get pods -A`. The same command is the usual first check when debugging workloads.

## Further reading

- [Pods](https://kubernetes.io/docs/concepts/workloads/pods/) — core workload unit and how they relate to nodes.
- [kubectl Quick Reference](https://kubernetes.io/docs/reference/kubectl/quick-reference/) — common commands including listing workloads cluster-wide.
