{
  "name": "killercoda-scenarios",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint:md": "markdownlint-cli2 \"**/*.md\"",
    "lint:json": "python3 scripts/lint_json.py",
    "lint": "npm run lint:md && npm run lint:json"
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.12.1"
  }
}
