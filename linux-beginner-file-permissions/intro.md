
You will work as `root` in a disposable Ubuntu environment. You will practice **numeric `chmod`**, interpret `ls -l` permission bits, and use `stat` to confirm mode bits on files and directories.

```text
+-------------+       +---------------------------+
| chmod / ls  | ----> | owner group other (rwx)   |
+-------------+       +---------------------------+
```
