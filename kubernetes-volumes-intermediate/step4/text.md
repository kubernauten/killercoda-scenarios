**Update** `/my-vol/config.env` so it contains **`VERSION=2`** instead of **`VERSION=1`** (still a single line with that exact key/value).

Leave **`/my-vol/copy.txt`** as-is from the previous step (still reflecting the old content is OK).

Use **Check** when `config.env` has the updated value.
